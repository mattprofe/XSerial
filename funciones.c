//============= MBCORP
//=== Funciones para puertos  
//=== con la placa de entrenamiento 
//=== Matias Leonardo Baez


#include <pic.h>
#include "funciones.h"

/*
void pMode(unsigned pin, unsigned state){
	if (pin>=9)
	{
		if(state==OUTPUT)
			bit_clr(TRISA,pin);
		else
			bit_set(TRISA,pin);
	}
	else
	{
		if(state==OUTPUT)
			bit_clr(TRISB,pin);
		else
			bit_set(TRISB,pin);
	}
}

*/